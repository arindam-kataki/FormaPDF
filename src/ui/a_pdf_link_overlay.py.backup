#!/usr/bin/env python3
"""
Minimal PDF Link Overlay - Simple implementation to avoid import issues
"""

from PyQt6.QtWidgets import QWidget
from PyQt6.QtCore import pyqtSignal

class PDFLinkOverlayManager(QWidget):
    """Minimal overlay manager"""
    
    linkClicked = pyqtSignal(object)
    
    def __init__(self, parent=None):
        super().__init__(parent)
        self.link_manager = None
        print("📎 Minimal PDFLinkOverlayManager initialized")
    
    def set_link_manager(self, link_manager):
        """Set the link manager"""
        self.link_manager = link_manager
        print("✅ Link manager set")
    
    def update_page_links(self, page_num: int, zoom_level: float = 1.0):
        """Get timing output without complex overlays"""
        if self.link_manager and hasattr(self.link_manager, 'get_page_links_minimal'):
            result = self.link_manager.get_page_links_minimal(page_num)
            # This gives you the timing output you wanted!
            
    def clear_overlays(self):
        """Stub method"""
        pass